// commands/admin-commands.js - Admin command handlers
const { parseQuotedArguments } = require('../utils/helpers');

function handleDaftar(bot, msg, match) {
  // Your existing /daftar handler code here
  // (Copy from your previous index.js or use the one I provided earlier)
}

function handleLihatUser(bot, msg) {
  // Your existing /lihat_user handler code here
}

function handleHapusUser(bot, msg, match) {
  // Your existing /hapus_user handler code here
}

module.exports = {
  handleDaftar,
  handleLihatUser,
  handleHapusUser
};
